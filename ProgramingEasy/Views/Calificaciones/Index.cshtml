@model IEnumerable<ProgramingEasy.Models.Calificaciones>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.calificacion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.calificacion)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id_calificacion">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id_calificacion">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id_calificacion">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
